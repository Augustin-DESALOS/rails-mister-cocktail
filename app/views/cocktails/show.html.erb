 <div class="container">
   <h1><%= @cocktail.name %></h1>
  </div>
</div>
<h2>Ingredients :</h2>
<ul>
    <% @cocktail.doses.each do |dose| %>
  <li>
<%= dose.description %> of <%= dose.ingredient.name %> <%= link_to '🗑', dose_path(dose), method: :delete, data: { confirm: "Are you sure?" } %>
  </li>
<% end %>
</ul>
<%= link_to "➕ Add a new Ingredient", new_cocktail_dose_path(@cocktail)  %>